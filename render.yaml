services:
  - type: cron
    name: stargazing-index-job
    schedule: "*/360 * * * *"  # Every 6 hours
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./run_all.sh"
    